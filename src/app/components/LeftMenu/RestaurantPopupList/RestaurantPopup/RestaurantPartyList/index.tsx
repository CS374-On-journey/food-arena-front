import * as React from 'react';
import styled from 'styled-components/macro';

const Box = styled.div`
    width: 270px;
    height: 380px;
    background: white;
    border-radius: 20px;
`

export default function RestaurantPartyList(props) {
    const { restaurant } = props;
    
    return (
        <Box>
            
        </Box>
    )
}